//# add dynamic HTML Templating

//#for auto hot-reload the code changes during running, will script 'nodemon app.js'
//#will listen on port '3001' as app.listen(3001);
>npm start

//#will install 3 free HTML templating (ejs), (pug), (express-handlebars)
//#'--save' for  added the packages in 'package.json' -> 'dependencies' part
>npm install --save ejs pug express-handlebars

//#BUG template 'shop.pug' page, just auto complete 'html:5' , will genrate auto pug template including head and body
//# bug is like shortcut html elements without start or end angle brackets, just element name
//# css class name dot(.) 'button.btn-primary'
//# attibutes values in parentheses 'button.btn-primary(value="", att2="")'   
//# support dynamic binding data '#{pageTitle}'
//# support 'block' binding 'styles' or 'content' 


//#to use 'Handlebars' view engine, we did installed before above steps
>npm install --save express-handlebars
>npm i express-handlebars
//#support normal HTML brackets elements
//#handlebars template support double brackets binding {{pageTitle}}
//##
    {{#if products.length > 0}} //  NOT SUPPORTED, you need use nodejs backend bool variables instead
    {{else}}
        <h1>else part<h1>
    {{/if}}
//##
//#no 'block' binding just use {{#if boolean}}YOUR CODE HERE{{/if}}

//#'ejs' view template not need required library
//##
    <title><%= pageTitle%></title>
//##

